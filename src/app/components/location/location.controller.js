'use strict';

angular.module('instastore')
    .controller('LocationIndex', ['$scope', 'rest', 'toaster', '$sce', '$filter', function($scope, rest, toaster, $sce, $filter) {
        console.log('Location Controller Initialized');
    }]);